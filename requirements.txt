# Multi-agent pipeline + Flask dashboard.
# Versions pinned to current installed minor; bump deliberately.

# LLM framework
agno>=2.5,<3.0
openai>=2.0,<3.0

# Search tools
duckduckgo-search>=8.1,<9.0
tavily-python>=0.7,<1.0

# Pipeline core
pydantic>=2.10,<3.0
json-repair>=0.50,<1.0
python-dotenv>=1.0,<2.0
httpx>=0.27,<1.0

# Web layer (Flask + HTMX)
flask>=3.1,<4.0
markupsafe>=3.0,<4.0

# PDF generation (download endpoint)
reportlab>=4.5,<5.0
# Spanish hyphenation for justified body text (avoids ugly word gaps)
pyphen>=0.14,<1.0

# Legacy / optional (FastAPI + Streamlit kept until dead code is removed)
fastapi>=0.115,<1.0
uvicorn>=0.40,<1.0
streamlit>=1.50,<2.0
